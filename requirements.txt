# OmniAI Python Dependencies
# Core HTTP Server (server.py) requires ONLY Python 3.8+ Standard Library (http.server, urllib, socketserver, json, re).
# The packages below are optional helper libraries for environment variable management and testing.

requests>=2.31.0
urllib3>=2.0.0
python-dotenv>=1.0.0
